# To re-generate default graphs

* For help

	node defaultGraphGenerator.js --help

* For generating **default.graph**

	node defaultGraphGenerator.js -c

* For generating **default_three_auths.graph**

	node defaultGraphGenerator.js -b -n 3 -o default_three_auths.graph

* For generating **default_three_auths_backup_to_all.graph**

	node defaultGraphGenerator.js -b -a -n 3 -o default_three_auths_backup_to_all.graph
	
* For generating **file_sharing.graph**

	node defaultGraphGenerator.js -f -o file_sharing.graph

## Graph types

* `default.graph`

	Default example SST topology containing two Auths (`Auth101` and `Auth102`) and their registered client/server entities across two separate networks (`net1` and `net2`).

* `privilege.graph`

	Multi-level delegated-access example topology used for automated privilege grand and revoke workflows.
  * Example entities include:
    * `Node0` ~ `Node6`
    * `ResourceA`, `ResourceB`, `ResourceC`, `ResourceD`
  * This graph is used together with `autoPrivilege.js` under [`entity/node/example_entities`](../../entity/node/example_entities).
  * Generate with 
    ```
    node defaultGraphGenerator.js -p -o privilege.graph
    ```

* `context_based.graph`

	Example topology demonstrating **context-based authorization** via the `Context` column in the `communication_policy` table.
  * Single Auth (`Auth101`, ID 101) with four entities across `net1`:
    * `net1.client` (group: `Clients`)
    * `net1.server` (group: `Servers`)
    * `net1.adminClient` (group: `AdminClients`)
    * `net1.adminServer` (group: `AdminServers`)
  * Contains two inline `commPolicies` entries defined directly in the graph file:
    * **Clients → Servers**: enforces all three context condition types:
      * `"Number of People"`: numeric upper bound (`Max: 3`)
      * `"Location"`: allowlist check (`Allowed: ["Classroom", "Meeting Room"]`)
      * `"Time of Day"`: time-range check (`Min: "09:00"`, `Max: "18:00"` in HH:MM format)
    * **AdminClients → AdminServers**: `Context: null` (no context restriction)
  * Entities must include a `"context"` key in the purpose JSON of their session key request:
    ```json
    {
      "group": "Servers",
      "context": {
        "Number of People": 2,
        "Location": "Classroom",
        "Time of Day": "10:30"
      }
    }
    ```
    Auth denies the request if any condition is not satisfied.
  * `net1.client` carries a `contextList` of 10 pre-crafted contexts (mix of GRANT and DENY) in its generated `.config` file. Each `initComm` call in `client.js` consumes the next context from the list automatically:

    | # | People | Location | Time of Day | Expected |
    |---|--------|----------|-------------|----------|
    | 1 | 2 | Classroom | 10:30 | **GRANT** |
    | 2 | 1 | Meeting Room | 14:00 | **GRANT** |
    | 3 | 4 | Classroom | 12:00 | DENY (too many people) |
    | 4 | 2 | Cafeteria | 11:00 | DENY (location not allowed) |
    | 5 | 1 | Classroom | 08:30 | DENY (too early) |
    | 6 | 3 | Classroom | 09:00 | **GRANT** (boundary: exact limits) |
    | 7 | 2 | Meeting Room | 19:00 | DENY (too late) |
    | 8 | 3 | Meeting Room | 17:59 | **GRANT** (boundary: just before max) |
    | 9 | 5 | Office | 23:00 | DENY (all conditions violated) |
    | 10 | 2 | Classroom | 18:01 | DENY (just over max time) |

  * Use `initComm net1.server` (for `net1.client`) or `initComm net1.adminServer` (for `net1.adminClient`) — the `group` field in the generated `targetServerInfoList` maps the entity name to the correct policy `Target` group.
  * Type `help` in the client or server prompt to list all available commands.
  * This graph is hand-authored (not generated by `defaultGraphGenerator.js`); generate credentials and Auth DB with:
    ```
    cd $ROOT/examples
    ./generateAll.sh configs/context_based.graph
    ```
