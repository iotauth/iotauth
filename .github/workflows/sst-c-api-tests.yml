name: sst-c-api Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  sst-c-api-tests:
    uses: iotauth/sst-c-api/.github/workflows/ci.yml@ci-fix
    with:
      iotauth-ref: ${{ github.sha }}
