// entityInfo (name, group, private key path)
entityInfo		net2.client		Clients		../../credentials/keys/net2/ClientKey.pem

// authInfo (id, host, port, public key path)
authInfo		102 	localhost		22900		../../auth_certs/Auth102EntityCert.pem

// List of targetServerInfo (name, host, port) - default host & port to which the client connects to
// this can be more than one
targetServerInfo		net1.server 	localhost	21100
targetServerInfo		net2.server 	localhost	22100
